# Convex Backend

This folder is reserved for Convex functions and schema.

Suggested next steps:
1. Run `bun run convex:dev`.
2. Follow Convex init prompts for this repo.
3. Add auth/data functions here and point `CONVEX_SITE_URL` to your deployment URL.
